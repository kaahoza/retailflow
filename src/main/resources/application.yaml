spring:
  application:
    name: retailflow

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/retailflow_db}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:Qhawe&Khanyisa_}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format-sql: true

  flyway:
    enabled: true
    validate-on-migrate: true
    baseline-on-migrate: false

jwt:
  secret: ${JWT_SECRET:retailflow-local-dev-secret-key-change-this-1234567890}